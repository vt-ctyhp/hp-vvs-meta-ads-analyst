; React 19 + Visx (which still pins react@^16-18 as a peer dep) need legacy
; peer-dep resolution. cmdk + vaul rely on radix-ui packages with the same
; constraint. Until those publish React 19-aware peer ranges this stays.
legacy-peer-deps=true
