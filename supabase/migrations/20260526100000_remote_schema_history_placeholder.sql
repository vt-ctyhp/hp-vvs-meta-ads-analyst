-- Remote schema history placeholder.
--
-- This migration version is already present in the linked Supabase project's
-- migration history, but no matching SQL file exists in this repository or in
-- local git worktrees. Keep the version in source control so future
-- `supabase db push --linked` runs can compare local and remote migration
-- ledgers without requiring a migration-history repair.

